#Requires AutoHotkey v2.0-beta.10
#warn all, off

InputBox(enter something, input title, "Xn Yn", Default)
;InputBox(Prompt, Title, Options, Default)
msgbox("hello")
msgbox()
funky()

funky(){
    age := 19
    infos("this will run")
    outputdebug("info")

}
;file
